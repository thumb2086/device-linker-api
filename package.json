{
  "name": "device-linker-monolith",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "postbuild": "cp apps/web/dist/index.html ./index.html && cp -r apps/web/dist/assets ./assets || true",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^8.57.0",
    "tsx": "^4.7.0"
  }
}
